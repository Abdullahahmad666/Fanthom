/**
 * X/Twitter card. Same artwork as the Open Graph image -- re-exported rather
 * than duplicated, so the two can never drift apart.
 */
export { default, alt, size, contentType } from "./opengraph-image";
